/* ============================================================================
 * PayrollHrExtraModals.tsx — modals ported from the original page 3.4.html.
 * ----------------------------------------------------------------------------
 * AUTO-GENERATED by port_missing_modals.py — regenerate rather than hand-edit.
 *
 * These dialogs exist in the legacy HTML and were reachable there via
 * onclick="openModal('…')", but were never carried into the React port, so the
 * buttons that target them did nothing. Markup, titles, sizes, tables and
 * footer actions are ported from the source.
 *
 * Legacy bridges applied:
 *   openModal('x')        -> onOpen('x')
 *   data-bs-dismiss       -> onClose
 *   processAction(...)    -> doAction(...)  (spinner -> receipt)
 * ========================================================================== */
import type { ReactNode } from "react";
import { useEffect, useRef, useState } from "react";
import styles from "../styles/payroll-hr.module.css";

const s = styles as Record<string, string>;
const cx = (...c: (string | false | undefined)[]) =>
	c.filter(Boolean).join(" ");

interface Props {
	active: string | null;
	onClose: () => void;
	onOpen: (id: string) => void;
}

interface Result {
	msg: string;
	ref?: string;
}

/* Modal shell — mirrors the shared kit used by the hand-written modals so
 * these dialogs look and behave identically. */
function MBox({
	id,
	active,
	title,
	size = "md",
	onClose,
	children,
	footer,
}: {
	id: string;
	active: string | null;
	title: ReactNode;
	size?: "md" | "lg" | "xl";
	onClose: () => void;
	children: ReactNode;
	footer?: ReactNode;
}) {
	if (active !== id) return null;
	return (
		<>
			<div className={s.backdrop} onClick={onClose} />
			<div className={s.modalWrap} role="dialog" aria-modal="true">
				<div
					className={cx(
						s.modalBox,
						size === "lg" && s.modalBoxLg,
						size === "xl" && s.modalBoxXl,
					)}
				>
					<div className={s.modalHeader}>
						<h5 className={s.modalTitle}>{title}</h5>
						<button
							type="button"
							className="btn-close"
							aria-label="Close"
							onClick={onClose}
						/>
					</div>
					<div className={s.modalBody}>{children}</div>
					{footer && <div className={s.modalFooter}>{footer}</div>}
				</div>
			</div>
		</>
	);
}

function Busy() {
	return (
		<div className={s.loadingOv}>
			<div className={s.spinner} />
			<p className={s.loadingLabel}>Processing...</p>
		</div>
	);
}

export default function PayrollHrExtraModals({
	active,
	onClose,
	onOpen,
}: Props) {
	const [results, setResults] = useState<Record<string, Result>>({});
	const [busy, setBusy] = useState<string | null>(null);
	const [flows, setFlows] = useState<Record<string, number>>({});
	const timer = useRef<number | undefined>(undefined);

	/* LEGACY BRIDGE: cacheResettable() — clear state when the modal closes. */
	useEffect(() => {
		if (active === null) {
			setResults({});
			setBusy(null);
			setFlows({});
		}
	}, [active]);
	useEffect(() => () => window.clearTimeout(timer.current), []);

	/* LEGACY BRIDGE: processAction() — 1.2s spinner, then a receipt. */
	const doAction = (id: string, msg: string, ref?: string) => {
		setBusy(id);
		timer.current = window.setTimeout(() => {
			setResults((p) => ({ ...p, [id]: { msg, ref } }));
			setBusy(null);
		}, 1200);
	};

	const nextFlow = (key: string, total: number) => {
		const cur = flows[key] ?? 1;
		if (cur >= total) {
			onClose();
			return;
		}
		setFlows((p) => ({ ...p, [key]: cur + 1 }));
	};
	void nextFlow;

	const bodyFor = (id: string, content: ReactNode) => {
		if (busy === id) return <Busy />;
		const r = results[id];
		if (r)
			return (
				<div className={s.receipt}>
					<div className={s.receiptIcon}>
						<i className="bi bi-check-lg" />
					</div>
					<h5 style={{ fontWeight: 700, color: "var(--pm-accent)" }}>
						{r.msg}
					</h5>
					{r.ref && (
						<p style={{ fontSize: 12, color: "var(--pm-muted)" }}>
							Ref: {r.ref}
						</p>
					)}
				</div>
			);
		return content;
	};

	return (
		<>
			<MBox
				id="manageBenefitsModal"
				active={active}
				onClose={onClose}
				title={
					<>
						<i className="bi bi-heart-pulse me-2" /> Manage Employee Benefits
					</>
				}
				footer={
					<>
						<button type="button" className={s.btnPm} onClick={onClose}>
							Close
						</button>
						<button
							type="button"
							className={cx(s.btnPm, s.btnPmP)}
							onClick={onClose}
						>
							Save Benefits
						</button>
					</>
				}
			>
				{bodyFor(
					"manageBenefitsModal",
					<>
						<div className="d-flex align-items-center gap-3 mb-3">
							<div className={s.avatar} style={{ background: "#EF4444" }}>
								JS
							</div>
							<div>
								<strong>John Smith</strong>
								<div style={{ fontSize: 11, color: "var(--pm-muted)" }}>
									EMP-001
								</div>
							</div>
						</div>
						<div className={s.statusRow}>
							<div>
								<strong>Private Medical Insurance</strong>
								<div style={{ fontSize: 11, color: "var(--pm-muted)" }}>
									Jubilee Insurance (M+3)
								</div>
							</div>
							<div className="form-check form-switch">
								<input
									className="form-check-input"
									type="checkbox"
									defaultChecked
								/>
							</div>
						</div>
						<div className={s.statusRow}>
							<div>
								<strong>Gym Membership Allowance</strong>
								<div style={{ fontSize: 11, color: "var(--pm-muted)" }}>
									KES 5,000 untaxed benefit
								</div>
							</div>
							<div className="form-check form-switch">
								<input
									className="form-check-input"
									type="checkbox"
									defaultChecked
								/>
							</div>
						</div>
						<div className={s.statusRow}>
							<div>
								<strong>Car Loan Subsidy</strong>
								<div style={{ fontSize: 11, color: "var(--pm-muted)" }}>
									Company subsidized rate
								</div>
							</div>
							<div className="form-check form-switch">
								<input className="form-check-input" type="checkbox" />
							</div>
						</div>
					</>,
				)}
			</MBox>
			<MBox
				id="offboardEmployeeModal"
				active={active}
				onClose={onClose}
				title={
					<>
						<i className="bi bi-person-x text-danger me-2" /> Offboard Employee
					</>
				}
				footer={
					<>
						<button type="button" className={s.btnPm} onClick={onClose}>
							Cancel
						</button>
						<button
							type="button"
							className={cx(s.btnPm, s.btnPmD)}
							onClick={onClose}
						>
							Initiate Offboarding
						</button>
					</>
				}
			>
				{bodyFor(
					"offboardEmployeeModal",
					<>
						<div className="mb-3">
							<label className={s.formLabel}>Employee</label>
							<select className={s.formControl}>
								<option>Wanjiku Ndungu (EMP-004)</option>
							</select>
						</div>
						<div className="mb-3">
							<label className={s.formLabel}>Termination Date</label>
							<input
								type="date"
								className={s.formControl}
								defaultValue="2025-06-30"
							/>
						</div>
						<div className="mb-3">
							<label className={s.formLabel}>Final Settlement Processing</label>
							<div className="form-check">
								<input
									className="form-check-input"
									type="checkbox"
									defaultChecked
								/>
								<label className="form-check-label">
									Prorate final salary automatically
								</label>
							</div>
							<div className="form-check">
								<input
									className="form-check-input"
									type="checkbox"
									defaultChecked
								/>
								<label className="form-check-label">
									Pay out untaken leave balance
								</label>
							</div>
							<div className="form-check">
								<input className="form-check-input" type="checkbox" />
								<label className="form-check-label">
									Deduct outstanding loans/advances
								</label>
							</div>
						</div>
					</>,
				)}
			</MBox>
			<MBox
				id="payrollHistoryModal"
				active={active}
				size="xl"
				onClose={onClose}
				title={
					<>
						<i className="bi bi-clock-history me-2" /> Payroll History &
						Archives
					</>
				}
				footer={
					<>
						<button type="button" className={s.btnPm} onClick={onClose}>
							Close
						</button>
					</>
				}
			>
				{bodyFor(
					"payrollHistoryModal",
					<>
						<div className="d-flex gap-2 mb-3">
							<input
								type="month"
								className={cx(s.formControl, "w-auto")}
								defaultValue="2025-05"
							/>
							<button
								type="button"
								className={cx(s.btnPm, "pm-btn-outline")}
								onClick={onClose}
							>
								<i className="bi bi-search"></i>
							</button>
						</div>
						<div className="table-responsive">
							<table className={s.tbl}>
								<thead>
									<tr>
										<th>Period</th>
										<th>Run Type</th>
										<th>Employees</th>
										<th>Net Total</th>
										<th>Status</th>
										<th>P10 Return</th>
										<th>Action</th>
									</tr>
								</thead>
								<tbody>
									<tr>
										<td>May 2025</td>
										<td>Regular</td>
										<td>139</td>
										<td>KES 4.1M</td>
										<td>
											<span className={cx(s.badge, s.badgeS)}>Completed</span>
										</td>
										<td>
											<span className={cx(s.badge, s.badgeS)}>Filed</span>
										</td>
										<td>
											<button
												type="button"
												className={cx(s.btnPm, s.btnSm)}
												onClick={onClose}
											>
												View details
											</button>
										</td>
									</tr>
									<tr>
										<td>Apr 2025</td>
										<td>Regular</td>
										<td>138</td>
										<td>KES 4.05M</td>
										<td>
											<span className={cx(s.badge, s.badgeS)}>Completed</span>
										</td>
										<td>
											<span className={cx(s.badge, s.badgeS)}>Filed</span>
										</td>
										<td>
											<button
												type="button"
												className={cx(s.btnPm, s.btnSm)}
												onClick={onClose}
											>
												View details
											</button>
										</td>
									</tr>
									<tr>
										<td>Apr 2025</td>
										<td>Bonus</td>
										<td>15</td>
										<td>KES 0.5M</td>
										<td>
											<span className={cx(s.badge, s.badgeS)}>Completed</span>
										</td>
										<td>Merged</td>
										<td>
											<button
												type="button"
												className={cx(s.btnPm, s.btnSm)}
												onClick={onClose}
											>
												View details
											</button>
										</td>
									</tr>
								</tbody>
							</table>
						</div>
					</>,
				)}
			</MBox>
			<MBox
				id="quickActionModal"
				active={active}
				onClose={onClose}
				title={
					<>
						<i className="bi bi-lightning-charge me-2" /> Quick Update
					</>
				}
				footer={
					<>
						<button
							type="button"
							className={cx(s.btnPm, s.btnPmP)}
							onClick={onClose}
						>
							Close
						</button>
					</>
				}
			>
				{bodyFor(
					"quickActionModal",
					<>
						<i
							className="bi bi-check2-circle text-success"
							style={{ fontSize: 48 }}
						></i>
						<h5 className="mt-3" style={{ fontWeight: 700 }}>
							Ali Omondi - Profile Update
						</h5>
						<p style={{ fontSize: 13, color: "var(--pm-muted)" }}>
							M-Pesa number verified successfully via Safaricom API.
						</p>
					</>,
				)}
			</MBox>
			<MBox
				id="viewPayslipModal"
				active={active}
				onClose={onClose}
				title={
					<>
						<i className="bi bi-file-earmark-text me-2" /> Payslip Preview
					</>
				}
				footer={
					<>
						<button type="button" className={s.btnPm} onClick={onClose}>
							Close
						</button>
						<button
							type="button"
							className={cx(s.btnPm, "pm-btn-outline")}
							onClick={onClose}
						>
							<i className="bi bi-download"></i>PDF
						</button>
					</>
				}
			>
				{bodyFor(
					"viewPayslipModal",
					<>
						<div className="text-center mb-3">
							<strong>ACME CORP LTD</strong>
							<br />
							Payslip for June 2025
							<br />
							<span style={{ color: "var(--pm-muted)" }}>
								John Smith (EMP-001)
							</span>
						</div>
						<div className="d-flex justify-content-between border-bottom pb-1">
							<span>Basic Salary</span>
							<span>KES 120,000.00</span>
						</div>
						<div className="d-flex justify-content-between border-bottom py-1">
							<span>House Allowance</span>
							<span>KES 15,000.00</span>
						</div>
						<div className="d-flex justify-content-between py-1 bg-light fw-bold">
							<span>Gross Pay</span>
							<span>KES 135,000.00</span>
						</div>
						<div className="mt-2 fw-bold" style={{ color: "var(--pm-danger)" }}>
							Deductions
						</div>
						<div className="d-flex justify-content-between border-bottom pb-1">
							<span>PAYE Tax</span>
							<span>(KES 24,000.00)</span>
						</div>
						<div className="d-flex justify-content-between border-bottom py-1">
							<span>NSSF (Tier 1+2)</span>
							<span>(KES 1,080.00)</span>
						</div>
						<div className="d-flex justify-content-between border-bottom py-1">
							<span>SHIF (2.75%)</span>
							<span>(KES 3,712.50)</span>
						</div>
						<div className="d-flex justify-content-between border-bottom py-1">
							<span>Housing Levy (1.5%)</span>
							<span>(KES 2,025.00)</span>
						</div>
						<div
							className="d-flex justify-content-between py-1 mt-2 bg-light fw-bold"
							style={{ color: "var(--pm-accent)" }}
						>
							<span>NET PAY</span>
							<span>KES 104,182.50</span>
						</div>
					</>,
				)}
			</MBox>
		</>
	);
}
