import { createFileRoute } from '@tanstack/react-router';
import { Card40305ProgramInactive } from '../../../features/errors/components/Card40305ProgramInactive';

export const Route = createFileRoute('/error/card/card-403-05-program-inactive')({
	component: Card40305ProgramInactive,
});
